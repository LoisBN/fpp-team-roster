// TODO: Create a PlayerCard component that accepts these props:
// name: string, position: string, number: number, team: string, imageUrl: string
// Export it as default

// Hints:
// - Use TypeScript interface to define the props
// - Make it look like a player card with image, name, position, number, and team
// - Use Tailwind classes for styling
// - Consider using a grid or flex layout

export default function PlayerCard() {
  return (
    <div className="/* TODO: Add styling */">
      {/* TODO: Build the player card UI */}
    </div>
  );
}